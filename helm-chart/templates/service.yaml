apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-tfm-ai
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-tfm-ai
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
